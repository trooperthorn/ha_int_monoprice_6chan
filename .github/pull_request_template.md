## What changed and why

## Testing

- [ ] `ruff check custom_components tests scripts` passes
- [ ] `mypy` passes
- [ ] `pytest` passes
- [ ] `python scripts/build_release_artifacts.py --validate-only` prints the manifest version
- [ ] Verified against real hardware, or explain why that was not possible

## Checklist

- [ ] Updated `docs/` if this changes behavior, protocol assumptions, or the release process
- [ ] No secrets, real hostnames, or credentials in the diff
